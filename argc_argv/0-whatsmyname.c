#include <stdio.h>
#include "main.h"

/**
 * main - prints name of the program
 * @argc: counts of the arguments
 * @argv: array of pointers to the arguments
 * Return: 0
 */

int main(int __attribute__((unused)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
